spring:
  application:
    name: search
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
        group: StudyOnline
      config:
        namespace: dev
        group: StudyOnline
        file-extension: yaml

  profiles:
    active: dev

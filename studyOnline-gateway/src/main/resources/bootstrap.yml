spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: dev
        group: StudyOnline
      config:
        namespace: dev
        group: StudyOnline
        file-extension: yaml
        refresh-enabled: true
  profiles:
    active: dev
